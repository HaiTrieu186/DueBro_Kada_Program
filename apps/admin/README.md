# Due Bro — Web Admin Dashboard

**Stack:** Next.js (App Router), Tailwind CSS, Server Components / Route Handlers.
**Quyền truy cập:** Dành cho đội vận hành nội bộ (phân quyền qua custom claim `app_metadata.role = 'ops'`), kết nối qua `service_role` server-side.
